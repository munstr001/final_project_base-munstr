# Study Material Recommender

## 📌 Description

**Study Material Recommender** — это backend-сервис для персонализированной рекомендации учебных материалов студентам на основе их истории обучения, предпочтений и характеристик учебных ресурсов.

Проект решает реальную задачу в области **ИИ в образовании**:  
помогает студентам понять, **какие материалы изучать дальше**, а преподавателям — автоматизировать процесс подбора контента.

Рекомендации строятся с использованием **гибридного подхода**:
- collaborative filtering (на основе поведения похожих студентов),
- content-based filtering (на основе тематики и сложности материалов).

---

## 🎯 Practical Use Case

В онлайн-курсах и LMS (Moodle, Canvas, Stepik и т.д.) студенты часто сталкиваются с большим количеством материалов и не понимают, что изучать дальше.

Данный сервис может быть встроен в LMS и использоваться для:
- персонализированных рекомендаций учебных материалов,
- поддержки студентов с разным уровнем подготовки,
- повышения вовлечённости и эффективности обучения.

---

## 🧠 Recommendation Approaches

### 1. Collaborative Filtering
- Анализирует оценки материалов другими студентами
- Использует item-item similarity (cosine similarity)
- Рекомендует материалы, которые понравились похожим студентам

### 2. Content-Based Filtering
- Учитывает характеристики материалов (topic, difficulty)
- Позволяет рекомендовать материалы даже при малом количестве оценок

### 3. Hybrid Model
- Объединяет результаты обоих подходов
- Повышает устойчивость и качество рекомендаций

---

## 📊 Evaluation Metrics

Для оценки качества рекомендаций используются:
- **RMSE** — ошибка предсказания рейтингов
- **Precision@K** — доля релевантных рекомендаций среди топ-K

Метрики автоматически пересчитываются в CI/CD workflow и сохраняются как artifacts.

---

## 🏗 Project Structure

.
├── src/
│ ├── recommender/
│ │ ├── collaborative.py
│ │ ├── content_based.py
│ │ ├── hybrid.py
│ │ └── metrics.py
│ └── api/
│ └── app.py
├── tests/
│ └── test_collaborative.py
├── data/
│ ├── ratings.csv
│ └── materials.csv
├── models/
├── reports/
├── .github/workflows/
│ ├── tests.yml
│ └── retrain.yml
├── README.md
├── requirements.txt
└── .gitignore

---

## ⚙️ Installation

### Requirements
- Python 3.9+
- pip

### Setup

```bash
git clone https://github.com/<username>/study-material-recommender
cd study-material-recommender
python -m venv venv
source venv/bin/activate   # Windows: venv\Scripts\activate
pip install -r requirements.txt
```

---

## 🚀 Usage

### Run API locally

```bash
python -m uvicorn src.api.app:app --reload
```

Open in browser:

* API docs: [http://127.0.0.1:8000/docs](http://127.0.0.1:8000/docs)

### Example request

```
GET /recommend/student/3
```

### Example response

```json
{
  "student_id": 3,
  "recommendations": [
    {
      "material_id": 2,
      "title": "Advanced Python",
      "topic": "python",
      "difficulty": 3,
      "score": 0.91,
      "reason": "Recommended based on similar students learning patterns"
    }
  ]
}
```

---

## 🧪 Testing

Run unit tests locally:

```bash
pytest
```

Tests are automatically executed in CI/CD on every push and pull request.

---

